---
phase: 03-core-extension-features
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified: ["manifest.json", "src/content.ts", "src/background.ts"]
autonomous: true
requirements: ["CONT-STRUC"]

must_haves:
  truths:
    - "Content script is registered in manifest.json"
    - "Background script acts as a proxy for messages between Sidebar and Content Script"
  artifacts:
    - path: "src/content.ts"
      provides: "Foundational content script listener"
---

<objective>
Set up the foundational content script and message proxy architecture.

Purpose: Preparation for Phase 4 text expansion and cross-context communication.
Output: Registered content script and working IPC bridge.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-core-extension-features/03-CONTEXT.md
@.planning/phases/03-core-extension-features/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register Content Script in Manifest</name>
  <files>manifest.json</files>
  <action>
  1. Add `content_scripts` to `manifest.json`.
  2. Map `matches: ["<all_urls>"]` to `js: ["src/content.ts"]`.
  3. Ensure `run_at: "document_idle"`.
  </action>
  <verify>dist/manifest.json contains content_scripts entry</verify>
  <done>Content script registered</done>
</task>

<task type="auto">
  <name>Task 2: Implement Content Script & Background Proxy</name>
  <files>src/content.ts, src/background.ts</files>
  <action>
  1. Create `src/content.ts` with a simple `onMessage` listener that logs "ClipStaff Content Script Ready".
  2. Update `src/background.ts` with the Message Proxy logic (listen for sidebar messages and forward to active tab).
  </action>
  <verify>npm run build; check logs for proxy activity</verify>
  <done>IPC Bridge implemented</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
</verification>

<success_criteria>
- Content script loads on web pages.
- Sidebar can trigger a message that the content script receives via the background worker.
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-extension-features/03-03-SUMMARY.md`
</output>
