---
phase: 03-core-extension-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["package.json", "src/App.tsx", "src/lib/clipboard.ts"]
autonomous: true
requirements: ["TOAST-INST", "ACTN-01"]

must_haves:
  truths:
    - "Sonner is installed and Toaster is mounted in App"
    - "Clipboard utility trims whitespace and triggers toasts"
  artifacts:
    - path: "src/lib/clipboard.ts"
      provides: "Unified clipboard copy function"
---

<objective>
Install notification infrastructure and implement the core clipboard utility.

Purpose: Foundation for user feedback and consistent clipboard writing.
Output: Integrated toast system and a reusable clipboard utility function.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-core-extension-features/03-CONTEXT.md
@.planning/phases/03-core-extension-features/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Sonner and mount Toaster</name>
  <files>package.json, src/App.tsx</files>
  <action>
  1. Install `sonner`.
  2. Mount `<Toaster />` at the root of `App.tsx` (inside AuthProvider).
  3. Configure Toaster position to `bottom-center` and set a 2000ms duration.
  </action>
  <verify>npm run build</verify>
  <done>Toast system is ready</done>
</task>

<task type="auto">
  <name>Task 2: Implement Clipboard Utility</name>
  <files>src/lib/clipboard.ts</files>
  <action>
  1. Create `src/lib/clipboard.ts`.
  2. Implement `copyToClipboard(text: string, label: string)` function.
  3. Action: Trim text -> `navigator.clipboard.writeText` -> `toast.success(`${label} copied!`)`.
  4. Include error handling and `toast.error` fallback.
  </action>
  <verify>tsc --noEmit</verify>
  <done>Clipboard utility is ready</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
</verification>

<success_criteria>
- Sonner toasts are styled and appearing in the correct location.
- Clipboard utility is used consistently across the app.
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-extension-features/03-01-SUMMARY.md`
</output>
