---
phase: 03-core-extension-features
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: ["src/components/ProfileList.tsx", "src/components/SnippetList.tsx"]
autonomous: true
requirements: ["ACTN-01", "SIDE-03"]

must_haves:
  truths:
    - "Clicking profile fields triggers the clipboard copy utility"
    - "Visual feedback (Check icon) is shown for 2 seconds after copy"
  artifacts:
    - path: "src/components/ProfileList.tsx"
      provides: "Updated profile list with copy-to-clipboard fields"
---

<objective>
Integrate click-to-copy into the Profile and Snippet lists with visual feedback.

Purpose: Main user interaction point for the recruitment clipboard.
Output: Enhanced UI components with immediate copy actions and local feedback.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-core-extension-features/03-CONTEXT.md
@.planning/phases/03-core-extension-features/03-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ProfileList with Copy Actions</name>
  <files>src/components/ProfileList.tsx</files>
  <read_first>src/lib/clipboard.ts</read_first>
  <action>
  1. Add a "Field Detail" expansion or a compact grid showing current active profile fields.
  2. For each field (Email, Phone, LinkedIn, etc.), add a clickable row or button.
  3. Action: Call `copyToClipboard` and manage local `isCopied` state for icon swapping.
  </action>
  <verify>Manual visual check; click field and verify toast + icon swap</verify>
  <done>Profiles have click-to-copy feedback</done>
</task>

<task type="auto">
  <name>Task 2: Standardize Snippet Copy Feedback</name>
  <files>src/components/SnippetList.tsx</files>
  <read_first>src/lib/clipboard.ts</read_first>
  <action>
  1. Refactor `SnippetList` to use the unified `copyToClipboard` utility.
  2. Ensure the existing icon swap logic matches the new `ProfileList` pattern.
  </action>
  <verify>Manual visual check; verify toast appears for snippet copy</verify>
  <done>Snippets have click-to-copy feedback</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
</verification>

<success_criteria>
- All active profile fields are one-click copyable.
- Visual feedback is consistent across all copy actions.
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-extension-features/03-02-SUMMARY.md`
</output>
