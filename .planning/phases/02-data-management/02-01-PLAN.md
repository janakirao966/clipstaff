---
phase: 02-data-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["supabase/migrations/20260515000000_init_schema.sql"]
autonomous: true
requirements: ["DB-INIT", "RLS-POL"]

must_haves:
  truths:
    - "Profiles and Snippets tables exist in public schema"
    - "RLS is enabled and restricts access to owner"
  artifacts:
    - path: "supabase/migrations/20260515000000_init_schema.sql"
      provides: "Schema and RLS definitions"
---

<objective>
Initialize the Supabase database schema and Row Level Security (RLS) policies.

Purpose: Provides the persistent storage and security foundation for profiles and snippets.
Output: SQL migration file with table definitions and security policies.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-data-management/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SQL migration for Profiles and Snippets</name>
  <files>supabase/migrations/20260515000000_init_schema.sql</files>
  <action>
  1. Create the `supabase/migrations` directory.
  2. Create `20260515000000_init_schema.sql`.
  3. Define `profiles` table with: id (uuid), user_id (uuid, auth.uid()), name, full_name, email, phone, linkedin_url, portfolio_url, location, visa_status, notice_period, is_active (bool), updated_at.
  4. Define `snippets` table with: id (uuid), user_id (uuid, auth.uid()), shortcut, text, category, created_at.
  5. Add unique constraint on `snippets(user_id, shortcut)`.
  6. Enable RLS on both tables.
  7. Add policies for ALL (CRUD) restricted to `auth.uid() = user_id`.
  </action>
  <verify>Check SQL file content for table and policy definitions</verify>
  <acceptance_criteria>
    - File contains `CREATE TABLE public.profiles`
    - File contains `CREATE TABLE public.snippets`
    - File contains `ALTER TABLE ... ENABLE ROW LEVEL SECURITY`
    - File contains `CREATE POLICY` with `USING (auth.uid() = user_id)`
  </acceptance_criteria>
  <done>SQL migration file exists with correct definitions</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] SQL file exists and is valid Postgres SQL
</verification>

<success_criteria>
- Schema definitions match the Phase 2 Context decisions.
- RLS policies ensure data isolation.
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-management/02-01-SUMMARY.md`
</output>
