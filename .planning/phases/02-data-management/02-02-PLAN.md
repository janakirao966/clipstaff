---
phase: 02-data-management
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: ["src/store/useStore.ts", "src/hooks/useProfiles.ts", "src/hooks/useSnippets.ts", "package.json"]
autonomous: true
requirements: ["PROF-01", "PROF-02", "PROF-03", "PROF-04", "PROF-05", "SNIP-01", "SNIP-02", "SNIP-03", "SNIP-04", "SNIP-05"]

must_haves:
  truths:
    - "Profiles and snippets can be fetched and updated via Zustand store"
    - "Active profile state is synchronized with the database"
  artifacts:
    - path: "src/store/useStore.ts"
      provides: "Zustand store with slices"
    - path: "src/hooks/useProfiles.ts"
      provides: "Hooks for profile CRUD"
    - path: "src/hooks/useSnippets.ts"
      provides: "Hooks for snippet CRUD"
---

<objective>
Implement the global state management and data fetching hooks.

Purpose: Bridges the UI with the Supabase backend and provides reactive state for the extension.
Output: Zustand store and custom React hooks for profile/snippet management.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-data-management/02-CONTEXT.md
@.planning/phases/02-data-management/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Vitest and setup testing infrastructure</name>
  <files>package.json, vitest.config.ts</files>
  <action>
  1. Add `vitest` to devDependencies in package.json.
  2. Create `vitest.config.ts` configured for React and with `@` alias.
  3. Add `test` script to package.json.
  </action>
  <verify>npm run test returns "no test files found" (exit 0 or 1 is fine as long as it runs)</verify>
  <done>Testing infrastructure is ready</done>
</task>

<task type="auto">
  <name>Task 2: Implement Zustand Store with Slices</name>
  <files>src/store/useStore.ts</files>
  <read_first>src/lib/supabase.ts</read_first>
  <action>
  1. Create `src/store/useStore.ts`.
  2. Implement `ProfileSlice` and `SnippetSlice` as defined in RESEARCH.md.
  3. Include actions for `setProfiles`, `setActiveProfile`, `setSnippets`, `setSearchTerm`.
  </action>
  <verify>tsc --noEmit passes</verify>
  <done>Global store is implemented</done>
</task>

<task type="auto">
  <name>Task 3: Implement Data Hooks for Profiles and Snippets</name>
  <files>src/hooks/useProfiles.ts, src/hooks/useSnippets.ts</files>
  <read_first>src/store/useStore.ts, src/lib/supabase.ts</read_first>
  <action>
  1. Create `src/hooks/useProfiles.ts`. Implement `fetchProfiles`, `createProfile`, `updateProfile`, `deleteProfile`. Handle the `is_active` toggle logic.
  2. Create `src/hooks/useSnippets.ts`. Implement `fetchSnippets`, `createSnippet`, `updateSnippet`, `deleteSnippet`.
  3. Ensure hooks update the Zustand store after successful Supabase calls.
  </action>
  <verify>tsc --noEmit passes</verify>
  <done>Data hooks are implemented and connected to store</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds (checks types)
</verification>

<success_criteria>
- All CRUD operations land in the Zustand store correctly.
- Types match the database schema from Plan 1.
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-management/02-02-SUMMARY.md`
</output>
