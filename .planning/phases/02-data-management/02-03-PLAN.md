---
phase: 02-data-management
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified: ["src/components/ProfileList.tsx", "src/components/SnippetList.tsx", "src/components/ProfileDetail.tsx", "src/components/SnippetDetail.tsx", "src/App.tsx"]
autonomous: true
requirements: ["SIDE-02", "SIDE-03", "SIDE-04"]

must_haves:
  truths:
    - "User can switch between Profile and Snippets tabs"
    - "Profiles and snippets are listed with Edit/Delete actions"
    - "Searching snippets works in real-time"
  artifacts:
    - path: "src/components/ProfileList.tsx"
      provides: "List of profiles with switching"
    - path: "src/components/SnippetList.tsx"
      provides: "Searchable list of snippets"
---

<objective>
Implement the management UI in the sidebar.

Purpose: Provides the user interface for all data management operations defined in the phase.
Output: React components for listing and editing profiles and snippets.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-data-management/02-CONTEXT.md
@.planning/phases/02-data-management/02-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Profile Components</name>
  <files>src/components/ProfileList.tsx, src/components/ProfileDetail.tsx</files>
  <action>
  1. Create `ProfileList.tsx`: Shows list of profiles with "Active" toggle and Edit/Delete buttons.
  2. Create `ProfileDetail.tsx`: Modal or slide-over form for editing all fields (email, phone, visa, etc.).
  </action>
  <verify>Manual visual check</verify>
  <done>Profile management UI is ready</done>
</task>

<task type="auto">
  <name>Task 2: Implement Snippet Components</name>
  <files>src/components/SnippetList.tsx, src/components/SnippetDetail.tsx</files>
  <action>
  1. Create `SnippetList.tsx`: Search bar at top + list of snippets (shortcut + snippet preview).
  2. Create `SnippetDetail.tsx`: Modal for creating/editing snippets (shortcut, text, category).
  </action>
  <verify>Manual visual check; Verify search filters the list</verify>
  <done>Snippet management UI is ready</done>
</task>

<task type="auto">
  <name>Task 3: Update App Layout with Tabs</name>
  <files>src/App.tsx</files>
  <action>
  1. Add a tab switcher (Profiles | Snippets) to the header or sub-header.
  2. Conditionally render `ProfileList` or `SnippetList` based on the active tab.
  </action>
  <verify>Switching tabs works and updates view</verify>
  <done>Navigation integrated into App</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
</verification>

<success_criteria>
- Navigation is smooth and state persists between tabs.
- Search functionality is instant.
- Form validation prevents empty shortcuts or duplicate names.
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-management/02-03-SUMMARY.md`
</output>
