---
phase: 04-shortcut-engine
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified: ["src/content.ts"]
autonomous: true
requirements: ["EXPN-02", "PERF-01"]

must_haves:
  truths:
    - "Shortcuts are instantly replaced with their expanded text"
    - "Native events are dispatched so web frameworks detect the change"
  artifacts:
    - path: "src/content.ts"
      provides: "Full-featured expansion engine"
---

<objective>
Implement the text expansion and replacement logic.

Purpose: Complete the core productivity loop.
Output: Production-ready expansion engine with framework compatibility.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-shortcut-engine/04-CONTEXT.md
@.planning/phases/04-shortcut-engine/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Replacement Logic</name>
  <files>src/content.ts</files>
  <action>
  1. Implement `expandShortcut(element, shortcut, expandedText)` function.
  2. Use `setRangeText()` to replace the specific range.
  3. Action: Prevent default behavior of the trigger key (Space/Enter) to avoid extra characters during expansion.
  4. Manually append the Space/Enter after expansion if necessary (or let it bubble after replacement).
  </action>
  <verify>npm run build</verify>
  <done>Replacement logic ready</done>
</task>

<task type="auto">
  <name>Task 2: Implement Framework Compatibility</name>
  <files>src/content.ts</files>
  <action>
  1. Dispatch `input` and `change` events after `setRangeText`.
  2. Ensure `bubbles: true` and `cancelable: true` are set.
  </action>
  <verify>npm run build</verify>
  <done>Expansion engine is compatible with React/Vue sites</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
</verification>

<success_criteria>
- Expansion is invisible to the user (no flickering).
- Undo (Ctrl+Z) works where supported by the browser/site.
- Expansion successfully populates fields on major job boards (mock tested or manual).
</success_criteria>

<output>
After completion, create `.planning/phases/04-shortcut-engine/04-03-SUMMARY.md`
</output>
