---
phase: 04-shortcut-engine
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified: ["src/content.ts"]
autonomous: true
requirements: ["EXPN-01"]

must_haves:
  truths:
    - "Content script monitors keydown events for Space and Enter"
    - "Parser correctly extracts the token starting with / before the cursor"
  artifacts:
    - path: "src/content.ts"
      provides: "Event-aware content script"
---

<objective>
Implement the shortcut detection logic in the content script.

Purpose: Identify expansion opportunities in real-time.
Output: Content script that triggers an expansion check on specific keystrokes.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-shortcut-engine/04-CONTEXT.md
@.planning/phases/04-shortcut-engine/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Keyboard Monitor</name>
  <files>src/content.ts</files>
  <action>
  1. Add a global `keydown` listener using event delegation.
  2. Filter for `Space` and `Enter`.
  3. Ensure the `event.target` is an `INPUT` or `TEXTAREA`.
  </action>
  <verify>npm run build</verify>
  <done>Keyboard monitoring active</done>
</task>

<task type="auto">
  <name>Task 2: Implement Shortcut Parser</name>
  <files>src/content.ts</files>
  <action>
  1. Implement `getShortcutAtCursor(element)` helper.
  2. Logic: Extract text from start of value to `selectionStart`.
  3. Use regex to find the trailing `/token`.
  4. Match the token against the `shortcutCache`.
  </action>
  <verify>npm run build</verify>
  <done>Token parser ready</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
</verification>

<success_criteria>
- The parser identifies valid shortcuts without false positives for normal slashes.
- Detection works in both single-line inputs and multi-line textareas.
</success_criteria>

<output>
After completion, create `.planning/phases/04-shortcut-engine/04-02-SUMMARY.md`
</output>
