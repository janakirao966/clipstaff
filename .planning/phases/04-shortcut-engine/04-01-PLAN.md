---
phase: 04-shortcut-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["src/content.ts", "src/background.ts"]
autonomous: true
requirements: ["EXPN-DATA"]

must_haves:
  truths:
    - "Content script maintains a local cache of all valid shortcuts"
    - "Background script provides the data slice on request"
  artifacts:
    - path: "src/content.ts"
      provides: "Sync-capable content script"
---

<objective>
Implement the data synchronization bridge between the extension and the content script.

Purpose: Zero-latency access to snippets for the expansion engine.
Output: Content script with a populated in-memory cache of shortcuts and profile fields.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-shortcut-engine/04-CONTEXT.md
@.planning/phases/04-shortcut-engine/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Background with Data Fetching</name>
  <files>src/background.ts</files>
  <action>
  1. Add a message listener for `SYNC_DATA` request.
  2. Action: Fetch snippets and active profile from Supabase (or cached in background if possible).
  3. Respond with a unified data object containing all shortcuts (e.g. `{ "/name": "John Doe", "/email": "john@example.com" }`).
  </action>
  <verify>tsc --noEmit</verify>
  <done>Background sync provider ready</done>
</task>

<task type="auto">
  <name>Task 2: Implement Content Script Cache</name>
  <files>src/content.ts</files>
  <action>
  1. Create a `shortcutCache` variable.
  2. Implementation a `syncData()` function that calls the background script.
  3. Trigger `syncData()` on load and on "PING" messages (indicating sidebar activity).
  </action>
  <verify>npm run build</verify>
  <done>Content script cache ready</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
</verification>

<success_criteria>
- Content script logs the number of shortcuts synced on startup.
- Cache is updated whenever the sidebar is opened/refreshed.
</success_criteria>

<output>
After completion, create `.planning/phases/04-shortcut-engine/04-01-SUMMARY.md`
</output>
