---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: ["src/lib/supabase.ts", "src/contexts/AuthContext.tsx", "src/components/Auth.tsx", "src/App.tsx", ".env.example", "src/background.ts", "manifest.json"]
autonomous: true
requirements: ["AUTH-01", "AUTH-02", "AUTH-03", "AUTH-04"]

must_haves:
  truths:
    - "User can log in and sign up"
    - "User session persists using chrome.storage.local"
  artifacts:
    - path: "src/lib/supabase.ts"
      provides: "Supabase client configured with chrome.storage.local"
      contains: "createClient"
    - path: "src/contexts/AuthContext.tsx"
      provides: "React Context for Auth state"
    - path: "src/components/Auth.tsx"
      provides: "Login/Signup UI"
    - path: "src/background.ts"
      provides: "Service worker to open side panel on action click"
      contains: "chrome.sidePanel.setPanelBehavior"
  key_links:
    - from: "src/App.tsx"
      to: "src/contexts/AuthContext.tsx"
      via: "wraps app in provider"
---

<objective>
Implement Supabase Authentication with Chrome Extension local storage persistence.

Purpose: Secures user data and allows personal profile management.
Output: Working authentication flow (Login/Signup) in the sidebar.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up Supabase Client and Service Worker</name>
  <files>src/lib/supabase.ts, .env.example, package.json, manifest.json, src/background.ts</files>
  <read_first>manifest.json</read_first>
  <action>
  1. Add `@supabase/supabase-js` to package.json.
  2. Create `src/lib/supabase.ts`. Initialize Supabase using `VITE_SUPABASE_URL` and `VITE_SUPABASE_ANON_KEY`.
  3. Configure Supabase auth to use a custom storage adapter that wraps `chrome.storage.local` to satisfy Chrome MV3 requirements (AUTH-04).
  4. Create `.env.example` defining `VITE_SUPABASE_URL` and `VITE_SUPABASE_ANON_KEY`.
  5. Create `src/background.ts`. Add listener: `chrome.sidePanel.setPanelBehavior({ openPanelOnActionClick: true })`.
  6. Update `manifest.json` to register `src/background.ts` as the background service worker.
  </action>
  <verify>npm run build passes without TypeScript errors</verify>
  <acceptance_criteria>
    - src/lib/supabase.ts contains `chrome.storage.local`
    - manifest.json contains `"background": {"service_worker"`
    - src/background.ts contains `openPanelOnActionClick`
  </acceptance_criteria>
  <done>Supabase client correctly configured with custom storage adapter</done>
</task>

<task type="auto">
  <name>Task 2: Implement AuthContext and Auth UI Components</name>
  <files>src/contexts/AuthContext.tsx, src/components/Auth.tsx, src/App.tsx</files>
  <read_first>src/lib/supabase.ts, src/App.tsx, .planning/phases/01-foundation/01-UI-SPEC.md</read_first>
  <action>
  1. Create `src/contexts/AuthContext.tsx` to expose `user`, `session`, `signIn`, `signUp`, and `signOut`. Listen to `supabase.auth.onAuthStateChange`.
  2. Create `src/components/Auth.tsx` with a login/signup form. Use Tailwind for styling conforming to 01-UI-SPEC.md. Include inputs for email and password, and buttons for "Log In" and "Create Profile".
  3. Update `src/App.tsx` to wrap the application in `<AuthProvider>`. If no user is logged in, render `<Auth />`. If a user is logged in, render a placeholder "Welcome [Email]" and a Logout button.
  </action>
  <verify>npm run build completes</verify>
  <acceptance_criteria>
    - src/contexts/AuthContext.tsx contains `createContext`
    - src/components/Auth.tsx contains form inputs for email and password
    - src/App.tsx imports and uses `<AuthProvider>`
  </acceptance_criteria>
  <done>Authentication UI state management is fully implemented</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] TypeScript types are valid
</verification>

<success_criteria>
- Supabase auth context manages session state correctly
- UI properly guards authenticated routes
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
