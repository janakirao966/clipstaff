---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["package.json", "vite.config.ts", "manifest.json", "index.html", "src/main.tsx", "src/App.tsx", "src/index.css"]
autonomous: true
requirements: ["SIDE-01"]

must_haves:
  truths:
    - "Extension loads in Chrome without errors"
    - "User can open the sidebar"
    - "Sidebar renders the React App"
  artifacts:
    - path: "manifest.json"
      provides: "Extension permissions and side panel config"
      contains: "\"sidePanel\""
    - path: "vite.config.ts"
      provides: "CRXJS build config"
      contains: "@crxjs/vite-plugin"
    - path: "src/App.tsx"
      provides: "Root UI Component"
  key_links:
    - from: "manifest.json"
      to: "index.html"
      via: "side_panel.default_path"
---

<objective>
Initialize the Vite MV3 Chrome Extension and set up the basic SidePanel UI.

Purpose: Establishes the foundational extension framework and makes the sidebar UI available to users.
Output: A buildable MV3 extension with React, Tailwind, and a working side panel.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Vite React app with CRXJS and Tailwind</name>
  <files>package.json, vite.config.ts, manifest.json, src/index.css, tailwind.config.js, postcss.config.js</files>
  <read_first>.planning/phases/01-foundation/01-RESEARCH.md</read_first>
  <action>
  1. Update package.json to include standard Vite + React dependencies, `@crxjs/vite-plugin@beta`, `lucide-react`, `tailwindcss`, `postcss`, `autoprefixer`.
  2. Create standard `tailwind.config.js` and `postcss.config.js`.
  3. Update `src/index.css` to include Tailwind directives (`@tailwind base; @tailwind components; @tailwind utilities;`) and design system CSS variables from UI-SPEC.md (Font: Inter, colors).
  4. Create `manifest.json` configured for MV3. Set `"manifest_version": 3`, name to "ClipStaff", `"permissions": ["sidePanel", "storage", "activeTab", "clipboardWrite"]`, and `"side_panel": {"default_path": "index.html"}`.
  5. Configure `vite.config.ts` to use `react()` and `crx({ manifest })` plugins.
  </action>
  <verify>npm run build completes without errors</verify>
  <acceptance_criteria>
    - package.json contains `@crxjs/vite-plugin`
    - manifest.json contains `"sidePanel"` permission
    - src/index.css contains `@tailwind base;`
  </acceptance_criteria>
  <done>Vite build succeeds and outputs to dist with manifest.json</done>
</task>

<task type="auto">
  <name>Task 2: Setup React Entry Point for Side Panel</name>
  <files>index.html, src/main.tsx, src/App.tsx</files>
  <read_first>manifest.json, src/index.css</read_first>
  <action>
  1. Configure `index.html` as the entry point for the Vite app.
  2. In `src/main.tsx`, render the `<App />` component into the root element.
  3. In `src/App.tsx`, build a minimal layout shell using Tailwind classes (w-full, h-screen, bg-slate-50, etc.). Include a header with "ClipStaff" and an empty content area.
  </action>
  <verify>npm run build succeeds</verify>
  <acceptance_criteria>
    - index.html references src/main.tsx
    - src/App.tsx exports a React component
  </acceptance_criteria>
  <done>React app builds correctly</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] `dist/manifest.json` exists
</verification>

<success_criteria>
- All tasks completed
- Extension skeleton is ready for Supabase Auth
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
